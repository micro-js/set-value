{
  "name": "@f/set-value",
  "description": "Set the value of an input-type DOM node",
  "repository": "git://github.com/micro-js/set-value.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/can-select-text": "^1.0.0"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
